<!doctype html>
<html>
  <head>
    <title>Display</title>
  </head>
  <body>
    <h1>Display</h1>
     <% meows.forEach(function(meow) { %>
        <div>Id: <%= meow._id %>, Name: <a href="/nameDisplay/<%= meow.name %>"><%= meow.name %></a></div>
     <% }); %>
  </body>
</html>
